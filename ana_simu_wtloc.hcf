@analyze_option{
    strategy = DFS;
    analysis_kind = simulate[slice = true, reset = true, multiply = false, loop max depth];
    local_analysis = true;
    goal = WeakPass;
    priorities = [simu=-1];
    memoize = false;
}